package com.example.teamprojectsolocode

import android.os.Bundle
import android.view.LayoutInflater
import android.view.View
import android.view.ViewGroup
import androidx.fragment.app.Fragment
import android.widget.TextView
import androidx.recyclerview.widget.LinearLayoutManager

class NoticeFragment : Fragment() {
/*
    private lateinit var noticeContentTextView: TextView

    override fun onCreateView(
        inflater: LayoutInflater, container: ViewGroup?,
        savedInstanceState: Bundle?
    ): View? {
        // Inflate the layout for this fragment
        val view = inflater.inflate(R.layout.fragment_group_info, container, false)

        // XML 레이아웃에서 View 참조
        noticeContentTextView = view.findViewById(R.id.noticeContentTextView)

        // 공지사항 초기화
        initializeNoticeRecyclerView()

        return view
    }

    private fun initializeNoticeRecyclerView() {
        // 가로로 된 RecyclerView 초기화
        val layoutManager = LinearLayoutManager(this, false)
        noticeRecyclerView.layoutManager = layoutManager

        // 예시 데이터 생성
        val noticeList = listOf("공지 1", "공지 2", "공지 3", "공지 4", "공지 5")

        // 어댑터 설정
        val adapter = InfoAdapter(noticeList)
        noticeRecyclerView.adapter = adapter
    }*/
}